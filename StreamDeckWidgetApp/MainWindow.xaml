<Window x:Class="StreamDeckWidgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:StreamDeckWidgetApp.Core.Converters"
        mc:Ignorable="d"
        Title="Virtual Deck"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:ImagePathToImageSourceConverter x:Key="PathToImage"/>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="15" BorderThickness="2">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#444"/>
                <Style.Triggers>
                    <!-- Editör açıkken kenarlık maviye dönsün -->
                    <DataTrigger Binding="{Binding IsEditorOpen}" Value="True">
                        <Setter Property="BorderBrush" Value="#4CC2FF"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="My Deck" Foreground="#555" VerticalAlignment="Center" FontWeight="Bold"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Editör Açma Butonu -->
                    <Button Command="{Binding OpenEditorCommand}" Content="⚙️" 
                            Width="25" Height="25" Margin="0,0,5,0"
                            Background="Transparent" Foreground="#888" BorderThickness="0" Cursor="Hand"/>
                    
                    <!-- Kapatma -->
                    <Button Command="{Binding CloseAppCommand}" Content="X" 
                            Width="25" Height="25"
                            Background="Transparent" Foreground="#888" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <!-- Butonlar (Sadece bu kaldı) -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding DeckItems}"
                              HorizontalAlignment="Left" VerticalAlignment="Top">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding DataContext.Columns, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                                    <Button Command="{Binding DataContext.ItemClickCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Margin="5" Cursor="Hand" BorderThickness="0"
                                        Width="{Binding DataContext.SelectedButtonSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                        Height="{Binding DataContext.SelectedButtonSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{Binding Color}" 
                                                            CornerRadius="8" 
                                                            Padding="5"
                                                            AllowDrop="True"
                                                            Drop="Button_Drop"
                                                            BorderThickness="3">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                                <Style.Triggers>
                                                                    <!-- Seçili buton vurgusu -->
                                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                        <Setter Property="BorderBrush" Value="#4CC2FF"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <Grid>
                                                            <Image Source="{Binding IconPath, Converter={StaticResource PathToImage}}" 
                                                                   Stretch="UniformToFill"
                                                                   Opacity="0.8"/>
                                                            
                                                            <TextBlock Text="{Binding Title}" 
                                                                       Foreground="White" 
                                                                       FontWeight="Bold"
                                                                       HorizontalAlignment="Center" 
                                                                       VerticalAlignment="Center"
                                                                       TextWrapping="Wrap" 
                                                                       TextAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
        </Grid>
    </Border>
</Window>
