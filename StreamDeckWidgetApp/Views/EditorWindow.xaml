<Window x:Class="StreamDeckWidgetApp.Views.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:StreamDeckWidgetApp.Core.Converters"
        xmlns:behaviors="clr-namespace:StreamDeckWidgetApp.Core.Behaviors"
        mc:Ignorable="d"
        Title="Stream Deck Control Center" Height="600" Width="900"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        behaviors:WindowDragBehavior.EnableDrag="True">

    <!-- Pencere Kenarlığı ve Gölge Efekti İçin -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" CornerRadius="10" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:ImagePathToImageSourceConverter x:Key="PathToImage"/>

        <!-- RENK PALETİ -->
        <SolidColorBrush x:Key="BgDark" Color="#121212"/>
        <SolidColorBrush x:Key="BgPanel" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BgInput" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="BorderColor" Color="#404040"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4CC2FF"/> <!-- Windows Blue -->
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#AAAAAA"/>

        <!-- MODERN TEXTBOX STİLİ -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- MODERN LABEL STİLİ -->
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>

        <!-- MODERN BUTTON STİLİ -->
        <Style TargetType="Button" x:Key="PrimaryBtn">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#121212"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- MODERN COMBOBOX STİLİ (DARK DROPDOWN) -->
        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3A3A3A"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Full ComboBox template to ensure Popup uses dark background -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch">
                                <Path x:Name="Arrow" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextPrimary}"/>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center" Margin="4,0,30,0"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{StaticResource BgPanel}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="6">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle" Value="{DynamicResource ComboBoxItemStyle}"/>
        </Style>
    </Window.Resources>

    <!-- ANA TASARIM -->
    <Border Background="{StaticResource BgDark}" CornerRadius="10" BorderBrush="#333" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/> <!-- SOL MENÜ -->
                <ColumnDefinition Width="*"/>   <!-- SAĞ İÇERİK -->
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- BAŞLIK -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 1. HEADER (SÜRÜKLENEBİLİR ALAN) -->
            <Grid Grid.ColumnSpan="2" Height="40" Background="Transparent">
                <TextBlock Text="Stream Deck Kontrol Merkezi" Foreground="#888" 
                           VerticalAlignment="Center" Margin="20,0,0,0" FontSize="12" IsHitTestVisible="False"/>
                
                <!-- Kayıt Durumu Göstergesi -->
                <TextBlock Text="{Binding SaveStatus}" Foreground="{StaticResource AccentColor}" 
                           VerticalAlignment="Center" HorizontalAlignment="Center" 
                           FontSize="11" FontWeight="SemiBold" IsHitTestVisible="False"/>
                
                <!-- Kapatma Butonu -->
                <Button Command="{Binding CloseCommand}" Content="X"  HorizontalAlignment="Right"
                            Width="40" Height="40"
                            Background="Transparent" Foreground="#888" BorderThickness="0" Cursor="Hand"/>
            </Grid>

            <!-- 2. SOL PANEL (SEKMELİ YAPIYI EKLE) -->
            <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource BgPanel}" 
                    CornerRadius="0,0,0,10" Padding="0">
                <TabControl Background="Transparent" BorderThickness="0">
                    <TabControl.Resources>
                        <!-- Tab Header Stili -->
                        <Style TargetType="TabItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="15,10"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderThickness="0,0,0,2" 
                                                BorderBrush="Transparent">
                                            <ContentPresenter ContentSource="Header" 
                                                              HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>

                    <!-- SEKME 1: AYARLAR -->
                    <TabItem Header="⚙️ AYARLAR">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="20">
                                <!-- Grid Yapılandırması -->
                                <Border Background="{StaticResource BgInput}" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                                    <StackPanel>
                                        <TextBlock Text="Grid Düzeni" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                                        
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="SATIR" FontSize="10" Foreground="#888" Margin="0,0,0,5"/>
                                                <TextBox Text="{Binding Rows, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="SÜTUN" FontSize="10" Foreground="#888" Margin="0,0,0,5"/>
                                                <TextBox Text="{Binding Columns, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- Buton Boyutu -->
                                <TextBlock Text="Buton Boyutu" Style="{StaticResource Label}"/>
                                <ComboBox ItemsSource="{Binding ButtonSizeOptions}" 
                                          SelectedValue="{Binding SelectedButtonSize}" 
                                          SelectedValuePath="Value" 
                                          DisplayMemberPath="Key"
                                          Height="40" Background="{StaticResource BgInput}" Foreground="White" Padding="10"/>

                                <Separator Background="#333" Margin="0,20"/>

                                <!-- Manuel Kaydet Butonu -->
                                <Button Command="{Binding SaveCommand}" Content="💾 ŞİMDİ KAYDET" Style="{StaticResource PrimaryBtn}"/>
                                
                                <TextBlock Text="Değişiklikler otomatik olarak kaydedilir." 
                                           Foreground="#555" FontSize="11" TextWrapping="Wrap" Margin="0,10,0,0" TextAlignment="Center"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- SEKME 2: KÜTÜPHANE -->
                    <TabItem Header="📚 KÜTÜPHANE">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Arama Çubuğu -->
                            <TextBox Grid.Row="0" 
                                     Text="{Binding DataContext.LibrarySearchText, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}"
                                     Background="{StaticResource BgInput}"
                                     Foreground="White"
                                     Padding="10"
                                     Margin="5,5,5,10"
                                     VerticalAlignment="Center">
                                <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <VisualBrush Stretch="None">
                                                            <VisualBrush.Visual>
                                                                <TextBlock Text="🔍 Ara..." Foreground="#666" FontSize="14" Margin="5,0,0,0"/>
                                                            </VisualBrush.Visual>
                                                        </VisualBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>

                            <!-- Kategori Filtresi -->
                            <ComboBox Grid.Row="1"
                                      ItemsSource="{Binding DataContext.LibraryCategories, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding DataContext.SelectedCategory, RelativeSource={RelativeSource AncestorType=Window}}"
                                      Height="35" 
                                      Background="{StaticResource BgInput}" 
                                      Foreground="White" 
                                      Padding="10"
                                      Margin="5,0,5,10"/>

                            <!-- Preset Listesi -->
                            <ListBox Grid.Row="2"
                                     ItemsSource="{Binding DataContext.LibraryItems, RelativeSource={RelativeSource AncestorType=Window}}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     Margin="5,0,5,5"
                                     PreviewMouseLeftButtonDown="PresetList_PreviewMouseLeftButtonDown"
                                     MouseMove="PresetList_MouseMove">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Margin" Value="0,0,0,5"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="6">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2A2A2A"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource BgInput}" 
                                                CornerRadius="6" 
                                                Padding="10"
                                                Cursor="Hand">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="40"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- İkon -->
                                                <Border Grid.Column="0" 
                                                        Background="{Binding Color}" 
                                                        CornerRadius="6" 
                                                        Width="35" 
                                                        Height="35"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Icon}" 
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="18" 
                                                               Foreground="White"
                                                               HorizontalAlignment="Center" 
                                                               VerticalAlignment="Center"/>
                                                </Border>

                                                <!-- Bilgiler -->
                                                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" 
                                                               Foreground="White" 
                                                               FontWeight="SemiBold"
                                                               FontSize="12"/>
                                                    <TextBlock Text="{Binding Description}" 
                                                               Foreground="#888" 
                                                               FontSize="10"
                                                               TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>

            <!-- 3. SAĞ PANEL (SANAL GRİD ÖNİZLEME + DETAY EDİTÖR) -->
            <Grid Grid.Row="1" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Önizleme Başlık -->
                    <RowDefinition Height="*"/>    <!-- Sanal Grid -->
                    <RowDefinition Height="Auto"/> <!-- Ayırıcı -->
                    <RowDefinition Height="300"/>  <!-- Detay Editör -->
                </Grid.RowDefinitions>

                <!-- ÜST: SANAL GRİD ÖNİZLEME -->
                <TextBlock Grid.Row="0" Text="🎨 WIDGET ÖNİZLEME" 
                           Foreground="{StaticResource AccentColor}" 
                           FontWeight="Bold" FontSize="14" 
                           Margin="30,20,30,10"/>

                <!-- Sanal Grid Container -->
                <Border Grid.Row="1" Background="#0A0A0A" Margin="30,0" CornerRadius="10" 
                        BorderBrush="#333" BorderThickness="1">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                            <ItemsControl ItemsSource="{Binding DeckItems}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="{Binding Columns}"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.ItemClickCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Margin="5" BorderThickness="0"
                                                Width="80" Height="80"
                                                AllowDrop="True"
                                                Drop="PreviewGrid_Drop"
                                                DragOver="PreviewGrid_DragOver">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="MainBorder"
                                                                        Background="{Binding Color}" 
                                                                        CornerRadius="8" 
                                                                        Padding="5"
                                                                        BorderThickness="2">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                                                            <!-- Boş slot için kesik çizgili border -->
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding Title}" Value="Boş">
                                                                                    <Setter Property="BorderBrush" Value="#444"/>
                                                                                    <Setter Property="BorderThickness" Value="2"/>
                                                                                    <Setter Property="Background" Value="Transparent"/>
                                                                                </DataTrigger>
                                                                                <!-- Seçili buton -->
                                                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                                                                    <Setter Property="BorderThickness" Value="3"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <Grid>
                                                                        <Image Source="{Binding IconPath, Converter={StaticResource PathToImage}}" 
                                                                               Stretch="UniformToFill" Opacity="0.8"/>
                                                                        
                                                                        <TextBlock Text="{Binding Title}" 
                                                                                   Foreground="White" 
                                                                                   FontWeight="Bold" FontSize="10"
                                                                                   HorizontalAlignment="Center" 
                                                                                   VerticalAlignment="Center"
                                                                                   TextWrapping="Wrap" 
                                                                                   TextAlignment="Center"/>
                                                                    </Grid>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="MainBorder" Property="Opacity" Value="0.8"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="Cursor" Value="Hand"/>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Viewbox>
                    </ScrollViewer>
                </Border>

                <!-- AYIRICI -->
                <Separator Grid.Row="2" Background="#333" Margin="30,10"/>

                <!-- ALT: DETAY EDİTÖR (SCROLLABLE) -->
                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="30,0,30,20">
                    <Grid>
                        <!-- BOŞ DURUM -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding SelectedDeckItem, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
                            <Border Background="{StaticResource BgPanel}" CornerRadius="50" Width="80" Height="80" Margin="0,0,0,15">
                                <TextBlock Text="👆" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Yukarıdaki önizlemeden bir buton seçin" 
                                       Foreground="#666" FontSize="14" FontWeight="SemiBold" TextAlignment="Center"/>
                        </StackPanel>

                        <!-- DOLU DURUM -->
                        <StackPanel DataContext="{Binding SelectedDeckItem}"
                                    Visibility="{Binding DataContext.SelectedDeckItem, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                        
                        <TextBlock Text="BUTON DÜZENLEME" Foreground="{StaticResource AccentColor}" FontWeight="Bold" FontSize="14" Margin="0,0,0,20"/>

                        <!-- Üst Kısım: İkon ve Renk -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- İkon Önizleme -->
                            <Border Grid.Column="0" Width="100" Height="100" Background="{StaticResource BgInput}" CornerRadius="15" Margin="0,0,20,0">
                                <Grid>
                                    <Image Source="{Binding IconPath, Converter={StaticResource PathToImage}}" Margin="15"/>
                                    <Border Background="#AA000000" VerticalAlignment="Bottom" Height="25" CornerRadius="0,0,15,15">
                                        <TextBlock Text="Sürükle Bırak" Foreground="White" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Renk ve Başlık -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Başlık" Style="{StaticResource Label}" Margin="0"/>
                                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Renk (Hex)" Style="{StaticResource Label}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Background="{Binding Color}" CornerRadius="4" Margin="0,0,10,0" BorderBrush="#555" BorderThickness="1"/>
                                    <TextBox Grid.Column="1" Text="{Binding Color, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                            </StackPanel>
                        </Grid>

                        <Separator Background="#333" Margin="0,20"/>

                        <!-- Aksiyon Ayarları -->
                        <TextBlock Text="Aksiyon Tipi" Style="{StaticResource Label}"/>
                        <ComboBox ItemsSource="{Binding DataContext.ActionTypes, RelativeSource={RelativeSource AncestorType=Window}}"
                                  SelectedItem="{Binding ActionType}"
                                  Height="40" Background="{StaticResource BgInput}" Foreground="White" Padding="10"/>
                        
                        <!-- Buton Davranışı -->
                        <TextBlock Text="Buton Davranışı" Style="{StaticResource Label}"/>
                        <ComboBox SelectedItem="{Binding BehaviorType, UpdateSourceTrigger=PropertyChanged}"
                                  Height="40" Background="{StaticResource BgInput}" Foreground="White" Padding="10">
                            <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Push</System:String>
                            <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Toggle</System:String>
                        </ComboBox>
                        
                        <Border Background="#2A2A2A" CornerRadius="5" Padding="10" Margin="0,5,0,15">
                            <StackPanel>
                                <TextBlock Foreground="#888" FontSize="11" Margin="0,0,0,3">
                                    <Run Text="• Push:" FontWeight="Bold"/> Tek seferlik aksiyon (Program aç, makro çalıştır)
                                </TextBlock>
                                <TextBlock Foreground="#888" FontSize="11">
                                    <Run Text="• Toggle:" FontWeight="Bold"/> Aç/Kapa mantığı (Sesi kapat, yayını başlat/durdur)
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Komut / Dosya Yolu / Hotkey" Style="{StaticResource Label}"/>
                        <TextBox Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="80" TextWrapping="Wrap" VerticalContentAlignment="Top" AcceptsReturn="True"/>

                        <Border Background="#2A2A2A" CornerRadius="5" Padding="10" Margin="0,10,0,0">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="💡 İpucu: " Foreground="{StaticResource AccentColor}" FontWeight="Bold"/>
                                    <TextBlock Text="Aksiyon tipine göre komut örnekleri:" Foreground="#AAA"/>
                                </StackPanel>
                                <TextBlock Foreground="#888" FontSize="11" Margin="15,0,0,2">
                                    <Run Text="• Execute:" FontWeight="Bold"/> C:\Program Files\app.exe veya https://google.com
                                </TextBlock>
                                <TextBlock Foreground="#888" FontSize="11" Margin="15,0,0,2">
                                    <Run Text="• Hotkey:" FontWeight="Bold"/> COPY, PASTE, SCREENSHOT, ALT+TAB
                                </TextBlock>
                                <TextBlock Foreground="#888" FontSize="11" Margin="15,0,0,0">
                                    <Run Text="• Drag &amp; Drop:" FontWeight="Bold"/> .exe, .lnk veya resim dosyası sürükleyin
                                </TextBlock>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>
